# ▸ EDIT THIS FOLDER — put your own sources here

The four files in this folder (`01-finance-close` … `04-cost-and-mix`) are a **demo** — one month of a fictional manufacturer.

**EDIT ▸ delete the demo files and drop in your own sources.** One file per source is best: your numbers, updates, and extracts (Markdown, CSV, pasted text, or exported PDF all work).

The one rule the agent obeys: it reads **everything in this folder and nothing outside it**, and every figure it uses must trace to a file in here. If a number is not in this folder, the agent will not use it.

*(This note is named with a leading underscore so the agent skips it — it is guidance, not a source. Keep it or delete it.)*
