# Signal: the freed team is now fully booked (Q2 2026)

*Example signal. This is the file the weekly review uses to flag that `assumptions/capacity.md` no longer holds, so the worked example resolves end to end.*

**What changed:** the team freed by exiting the public-tender practice (Sep 2025) is now fully booked on the private-sector engagement signed in May 2026. It is no longer spare.

**Touches:** `assumptions/capacity.md` (assumed the freed team stays absorbable / available), and `decisions/2025-09-exit-public-tender-practice.md` (the exit, which assumed the team could flex back).

**So what:** any move that counts on that team being available again, for example re-entering tenders for a flagship bid, is weaker than it looks. Flag at the next review.
